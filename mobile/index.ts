/**
 * index.ts — RN entry point
 *
 * Registers the App component. In Expo dev builds, this is referenced
 * by the generated native entry.
 */

import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
